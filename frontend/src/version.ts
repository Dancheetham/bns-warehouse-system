// Single source of truth for the version shown in small text next to
// "System" in the top-left branding - bump this with every delivered
// release so it's an easy visual check that an update actually went through
// (e.g. after deploying, confirm the number on screen matches what was sent).
export const APP_VERSION = "v0.096";
