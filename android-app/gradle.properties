# android.useAndroidX is required as soon as any androidx.* dependency is used
# (core-ktx, appcompat here) - without it, Gradle refuses to build with a
# clear error rather than silently doing the wrong thing.
android.useAndroidX=true
android.enableJetifier=true
